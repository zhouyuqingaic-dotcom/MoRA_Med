# MoRA-Med runtime dependencies
# Experimental environment: Python 3.11.15
#
# IMPORTANT (fresh environment):
# 1) Install PyTorch/torchvision first:
#    pip install torch==2.10.0+cu130 torchvision==0.25.0+cu130 \
#        --index-url https://download.pytorch.org/whl/cu130
# 2) Install FlashAttention after PyTorch:
#    pip install flash-attn==2.8.3 --no-build-isolation
# 3) Install the remaining dependencies:
#    pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu130

# PyTorch
torch==2.10.0+cu130
torchvision==0.25.0+cu130

# Hugging Face / Qwen3-VL
transformers @ git+https://github.com/huggingface/transformers.git@ca960f0cc0d2c2549b0f1834a51fa91230e50134
accelerate==1.13.0
peft==0.18.1
bitsandbytes==0.49.2
safetensors==0.7.0
flash-attn==2.8.3

# Vision / numerical
numpy==2.3.5
Pillow==12.0.0
open-clip-torch==2.23.0
timm==1.0.26

# Evaluation / utilities
openai==2.29.0
requests==2.32.5
tqdm==4.67.3
